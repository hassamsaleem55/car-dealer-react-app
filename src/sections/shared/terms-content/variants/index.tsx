import type { DealerSectionProps } from "@types-dir/dealer-props";
import TermsContentOne from "../layouts";

export function TermsContentDefault({ props: _props }: { props: DealerSectionProps }) {
  return <TermsContentOne />;
}
